package graphs.run;

import graphs.algoritm.Pair;
import graphs.model.Schema;

public class Run {

	public static void main(String[] args) {
		Schema sh = new Schema();
		sh.generateSchema(100, 200, 3, 5, 10);
		System.out.println(sh.toString());
		Pair p = new Pair(sh);
		p.calculate();
	}
}
