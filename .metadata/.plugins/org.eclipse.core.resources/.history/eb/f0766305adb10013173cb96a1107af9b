package graphs.run;

import graphs.algoritm.Pair;
import graphs.model.Schema;

public class Run {

	public static void main(String[] args) {
		Schema sh = new Schema();
		sh.generateSchema(10, 20, 3, 4, 2);
		System.out.println(sh.toString());
		Pair p = new Pair(sh);
		p.run
	}
}
